const isDisposableEmail = require('./is-disposable-email');
const isEmptyObject = require('./is-empty-object');
const isValidEmail = require('./is-valid-email');
const isValidJson = require('./is-valid-json');
const isConstructor = require('./is-consturctor');

module.exports = {
    isDisposableEmail,
    isEmptyObject,
    isValidEmail,
    isValidJson,
    isConstructor
};